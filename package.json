{
  "name": "@ravian0772/jenkins-mcp",
  "version": "1.0.0",
  "description": "MCP server for Jenkins — list jobs, trigger builds, check status, fetch logs",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "jenkins-mcp": "./build/index.js"
  },
  "files": ["build", "README.md"],
  "scripts": {
    "build": "tsc && chmod 755 build/index.js && node -e \"const fs=require('fs');const f='build/index.js';fs.writeFileSync(f,'#!/usr/bin/env node\\n'+fs.readFileSync(f,'utf8'))\"",
    "dev": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["mcp", "jenkins", "ci", "devops", "mcp-server"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/alysaleem/jenkins-mcp"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.0",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.8.3"
  }
}
